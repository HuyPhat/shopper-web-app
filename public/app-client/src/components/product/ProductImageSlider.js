import React from 'react'
import {Grid, Row, Col, Breadcrumb} from 'react-bootstrap'
import ProductImageSlider from "../product/ProductImageSliders"

const ProductImageSlider = React.createClass({
	render() {
		return (
			
		)
	}
});

export default ProductImageSlider;